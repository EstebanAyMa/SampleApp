- provide(:title, 'Nuevo Producto')

h1 Nuevo Crucero
.row
  .col-md-4.m-auto.card
    = form_for(@cruise, html: { multipart: true }) do |f|
      = render 'fields', f: f
      = f.submit "Agregar crucero", class: "btn btn-primary"
